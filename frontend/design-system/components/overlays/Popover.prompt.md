Anchored popover, e.g. the map "Layers" checklist.
```jsx
<div style={{position:"relative"}}><Button on>Layers (1 hidden)</Button>
  <Popover open><Check defaultChecked>Context</Check></Popover></div>
```
