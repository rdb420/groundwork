Modal for a single short question (e.g. the first-sign-in "What should we call you?" prompt).
```jsx
<Dialog title="What should we call you?" actions={<Button variant="primary">Save</Button>}>
  <Field label="Your name" />
</Dialog>
```
