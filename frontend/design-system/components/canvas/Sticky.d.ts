import * as React from "react";
/** Marker-yellow sticky note with a 2px ink under-shadow. The brand's signature "what really happens" note. */
export interface StickyProps { children?: React.ReactNode; width?: number; height?: number; rotate?: number; suggested?: boolean; }
export function Sticky(props: StickyProps): JSX.Element;
