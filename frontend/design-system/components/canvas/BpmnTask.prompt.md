A step on the map.
```jsx
<BpmnTask label="Check bank feed" taskKind="user" />
<BpmnTask label="Send arrears SMS" taskKind="system" tags={["workaround"]} />
<BpmnTask label="Chase late rent" sub />
```
Suggested (AI) elements are dashed survey blue until kept.
